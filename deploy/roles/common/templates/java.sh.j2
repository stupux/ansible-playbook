#!/bin/sh
export JAVA_HOME=/usr/lib/jvm/{{ 'java' if is_rpm else 'default' }}
if [ -z "$LD_LIBRARY_PATH" ]; then
  export LD_LIBRARY_PATH=$JAVA_HOME/jre/lib/amd64
else
  export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:$JAVA_HOME/jre/lib/amd64
fi
